import '../Organisation/style.css'

const inComplete = () => {
    return (
        <div className="border">
            <h1>Your profile won't be visible to volunteers until it is completed!</h1>
        </div>
    )
  }
  export default inComplete